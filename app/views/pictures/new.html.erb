<%= form_for(@picture, :html => { :multipart => true }) do |f| %>
  <div class="field">
        <%= f.text_field :caption %>
  </div>
  <div class="field">
        <%= f.file_field :avatar %>
  </div>
<%= f.hidden_field :kitty_id, { :value => params[:kitty_id] } %>
  <div class="actions">
    <%= f.submit "Upload!" %>
  </div>
<% end %>
